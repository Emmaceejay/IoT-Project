# DSGV Hub Firmware — Partition Table (8 MB flash)
#
# Layout: dual-bank OTA + isolated Matter NVS + SPIFFS storage
# Recommended for new hardware spins — no binary size pressure.
#
# ┌─────────────┬──────────┬─────────────────────────────────────────────────┐
# │ nvs         │  24 KB   │ App config, Wi-Fi creds, MQTT config, auth token│
# │ otadata     │   8 KB   │ Bootloader OTA state (which bank is active)     │
# │ phy_init    │   4 KB   │ RF / PHY calibration data                       │
# │ ota_0       │   3 MB   │ Active firmware (production slot)               │
# │ ota_1       │   3 MB   │ OTA update slot (bootloader rolls back on crash)│
# │ matter_nvs  │ 384 KB   │ Matter pairing & fabric data (ISOLATED)         │
# │ storage     │ 1.5 MB   │ SPIFFS — optional file storage for future use   │
# └─────────────┴──────────┴─────────────────────────────────────────────────┘
#
# matter_nvs is a separate NVS partition so that a Matter factory-reset
# (decommission from HomeKit / Google Home / Alexa) erases ONLY Matter pairing
# data — it does NOT touch Wi-Fi credentials or device config in the main nvs
# partition. Set CONFIG_CHIP_NVS_PARTITION_NAME="matter_nvs" to activate this.
#
# OTA slot headroom: 3,145,728 B slot − 3,080,192 B limit = 65,536 B (64 KB)
# Hard build limit enforced by scripts/check_binary_size.py.
#
# To use this table, add to the device sdkconfig.defaults:
#   CONFIG_PARTITION_TABLE_CUSTOM_FILENAME="../../partitions_8mb.csv"
#   CONFIG_ESPTOOLPY_FLASHSIZE_8MB=y
#
# Name,       Type, SubType,  Offset,   Size
nvs,          data, nvs,      0x9000,   0x6000
otadata,      data, ota,      0xf000,   0x2000
phy_init,     data, phy,      0x11000,  0x1000
ota_0,        app,  ota_0,    0x20000,  0x300000
ota_1,        app,  ota_1,    0x320000, 0x300000
matter_nvs,   data, nvs,      0x620000, 0x60000
storage,      data, spiffs,   0x680000, 0x180000
